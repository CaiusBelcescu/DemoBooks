# BookCard Demo

Small Vite + React demo showing a `BookCard` component using SWR and Tailwind.

Quick start

1. Install deps:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

Open http://localhost:5173
